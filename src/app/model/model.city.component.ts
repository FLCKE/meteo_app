export interface CityComponent {
    city: {
        country: string,
        insee: string,
        cp: Number,
        name: string,
        latitude: Number,
        longitude: Number,
        altitude: Number,
    }
}